services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      CONFIG_FILE: /config/config.json
    volumes:
      - ./config.json:/config/config.json:ro
      - ./data:/app/data
